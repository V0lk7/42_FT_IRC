#ifndef PENDINGACTION_HPP
# define PENDINGACTION_HPP

void    handleCommand( char* buffer );

/* 
 fonction qui parse le premier mot
 buffer faut le parser 1 mot determine un nouveau parsing
 */

#endif
